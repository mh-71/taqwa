---
import Layout from '../layouts/Layout.astro';
const base = import.meta.env.BASE_URL;
---

<Layout
  title="Contact Us - Taqwa Automobile Service Center"
  description="Contact Taqwa Automobile Service Center in Diabari, Uttara, Dhaka. Call, WhatsApp, or send a message to book LPG & CNG conversion, hybrid service, or any car repair."
  path="contact.html"
  current="contact"
>

<style>
  :root{
    /* ===== Brand colors ===== */
    --red:#F97316;
    --red-dark:#CE5805;
    --green:#16A34A;
    --ink:#101318;
    --charcoal:#162336;
    --charcoal-2:#1E304A;
    --line:#293C58;
    --paper:#F8FAFC;
    --muted:#9A9AA0;
    --off:#E9E9EC;

    /* ===== Surface tokens (used by dark/light theme toggle) ===== */
    --surface-0:#F8FAFC;   /* base page / header */
    --surface-1:#F1F3F7;   /* alternate section tint */
    --surface-2:#EEF1F6;   /* deeper alternate tint */
    --surface-3:#EBEEF3;   /* trust-strip / feature-strip fill */
    --text-strong:#101318; /* headings on light surfaces */
    --text-muted:#5a5a5e;  /* paragraphs on light surfaces */
    --card-bg:#EFF2F6;     /* card body fill on light surfaces */
    --surface-border:#EAEDF2; /* hairline borders on light surfaces */

    /* ===== Typography scale ===== */
    --font-display:'Poppins', "Arial Narrow", Arial, sans-serif;
    --font-body:'Inter', "Segoe UI", Arial, sans-serif;
    /* Fluid via clamp(): scales smoothly between a 375px and 1440px viewport
       instead of jumping at breakpoints. Layout still changes at @media steps;
       only type size and spacing flow continuously. */
    --text-xs:13px;
    --text-sm:14px;
    --text-base:15px;
    --text-md:16px;
    --text-lg:clamp(15px, 14.6px + 0.15vw, 16px);
    --text-xl:clamp(16px, 15.3px + 0.28vw, 18px);
    --text-2xl:clamp(18px, 16.5px + 0.6vw, 21px);
    --text-3xl:clamp(20px, 17.7px + 0.9vw, 24px);
    --text-4xl:clamp(24px, 20.2px + 1.5vw, 30px);
    --text-5xl:clamp(28px, 21.9px + 2.5vw, 40px);
    --text-6xl:clamp(32px, 23.4px + 3.4vw, 52px);

    /* ===== Spacing scale (8pt grid) ===== */
    --space-1:4px;
    --space-2:8px;
    --space-3:12px;
    --space-4:clamp(14px, 13.4px + 0.24vw, 16px);
    --space-5:clamp(18px, 16.5px + 0.6vw, 24px);
    --space-6:clamp(22px, 19.7px + 0.9vw, 32px);
    --space-7:clamp(32px, 28.1px + 1.5vw, 48px);
    --space-8:clamp(40px, 33.8px + 2.4vw, 64px);

    /* ===== Radius scale ===== */
    --radius-sm:3px;
    --radius-md:4px;
    --radius-lg:6px;
    --radius-xl:10px;
    --radius-full:50%;

    /* ===== Elevation / shadow scale ===== */
    --shadow-1:0 2px 6px rgba(0,0,0,0.08);
    --shadow-2:0 8px 20px -8px rgba(0,0,0,0.18);
    --shadow-3:0 14px 34px -18px rgba(0,0,0,0.3);
    --shadow-4:0 20px 50px -20px rgba(0,0,0,0.25);
    --shadow-5:0 30px 60px -20px rgba(0,0,0,0.6);
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    font-family: var(--font-body);
    background:var(--surface-1);
    color:var(--off);
    -webkit-font-smoothing:antialiased;
  }
  .display{
    font-family: var(--font-display);
    font-weight:800;
    letter-spacing:0.5px;
    text-transform:uppercase;
  }
  a{color:inherit; text-decoration:none;}
  ul{list-style:none;}
  button{font-family:inherit; cursor:pointer; border:none;}

  /* ===== ICON SPRITE UTILITY ===== */
  .icon{width:1em; height:1em; display:inline-block; vertical-align:-0.125em; flex-shrink:0;}

  /* ===== SCROLL-REVEAL MICRO-ANIMATIONS ===== */
  @keyframes fadeInUp{
    from{opacity:0; transform:translateY(28px);}
    to{opacity:1; transform:translateY(0);}
  }
  @keyframes floatBounce{
    0%,100%{transform:translateY(0);}
    50%{transform:translateY(-6px);}
  }
  @keyframes shimmer{
    0%{background-position:100% 0;}
    100%{background-position:0 0;}
  }
  .reveal{
    opacity:0;
    transform:translateY(28px);
    transition:opacity .7s cubic-bezier(.22,.61,.36,1), transform .7s cubic-bezier(.22,.61,.36,1);
    will-change:opacity, transform;
  }
  .reveal.is-visible{
    opacity:1;
    transform:translateY(0);
  }
  @media (prefers-reduced-motion: reduce){
    .reveal, .reveal.is-visible{opacity:1; transform:none; transition:none;}
    .floatBounce, .sticky-actions .fab{animation:none !important;}
  }

  /* ===== LAZY-LOAD FADE-IN (blur-up placeholder) ===== */
  .lazy-fade{opacity:0; transition:opacity .5s ease;}
  .lazy-fade.is-loaded{opacity:1;}

  /* ===== STICKY FLOATING WHATSAPP / CALL BUTTONS ===== */
  .sticky-actions{
    position:fixed; right:20px; bottom:20px; z-index:200;
    display:flex; flex-direction:column; align-items:flex-end; gap:12px;
  }
  .sticky-actions .fab{
    width:56px; height:56px; border-radius:var(--radius-full);
    display:flex; align-items:center; justify-content:center;
    color:#fff; box-shadow:var(--shadow-4);
    animation:floatBounce 2.6s ease-in-out infinite;
    position:relative;
  }
  .sticky-actions .fab .icon{width:26px; height:26px;}
  .sticky-actions .fab.fab-whatsapp{background:var(--green);}
  .sticky-actions .fab.fab-whatsapp:hover{background:#117F3A;}
  .sticky-actions .fab.fab-call{background:var(--red); animation-delay:.4s;}
  .sticky-actions .fab.fab-call:hover{background:var(--red-dark);}
  .sticky-actions .fab::after{
    content:""; position:absolute; inset:0; border-radius:var(--radius-full);
    box-shadow:0 0 0 0 rgba(22,163,74,0.45);
    animation:pulseRing 2.2s ease-out infinite;
    pointer-events:none;
  }
  .sticky-actions .fab.fab-call::after{box-shadow:0 0 0 0 rgba(249,115,22,0.45);}
  @keyframes pulseRing{
    0%{box-shadow:0 0 0 0 rgba(22,163,74,0.45);}
    70%{box-shadow:0 0 0 14px rgba(22,163,74,0);}
    100%{box-shadow:0 0 0 0 rgba(22,163,74,0);}
  }
  @media (max-width:760px){
    .sticky-actions{right:14px; bottom:14px; gap:10px;}
    .sticky-actions .fab{width:48px; height:48px;}
    .sticky-actions .fab .icon{width:22px; height:22px;}
  }

  /* ===== HEADER ===== */
  header{
    background:var(--surface-0);
    border-bottom:1px solid var(--surface-border);
    position:sticky; top:0; z-index:50;
  }
  .header-inner{
    max-width:1280px; margin:0 auto;
    display:flex; align-items:center; justify-content:space-between;
    padding:14px 32px;
  }
  .logo{display:flex; align-items:center;}
  .logo-img{height:46px; width:auto; display:block;}
  nav.main-nav{display:flex; align-items:center; gap:22px;}
  nav.main-nav > ul{display:flex; align-items:center; gap:18px;}
  nav.main-nav a{
    font-size:14.5px; font-weight:700; letter-spacing:0.4px;
    color:var(--text-strong); text-transform:uppercase; position:relative; padding:6px 0;
    display:flex; align-items:center; gap:4px; white-space:nowrap;
  }
  nav.main-nav li.active > a{color:var(--red);}
  nav.main-nav a.accent{color:var(--red);}
  nav.main-nav a:hover{color:var(--red);}
  .caret{width:11px; height:11px; flex-shrink:0; margin-top:1px; transition:transform .2s ease;}

  /* dropdown */
  .has-dropdown{position:relative;}
  .has-dropdown:hover > a .caret{transform:rotate(180deg);}
  .dropdown-menu{
    position:absolute; top:100%; left:-18px; margin-top:14px;
    background:var(--surface-0); min-width:220px; border-radius:var(--radius-md);
    box-shadow:0 18px 40px -12px rgba(0,0,0,0.25); border:1px solid var(--surface-border);
    padding:8px; opacity:0; visibility:hidden; transform:translateY(6px);
    transition:opacity .15s ease, transform .15s ease, visibility .15s;
    z-index:60;
  }
  .has-dropdown:hover .dropdown-menu{
    opacity:1; visibility:visible; transform:translateY(0);
  }
  .dropdown-menu::before{
    content:""; position:absolute; top:-6px; left:28px;
    width:12px; height:12px; background:var(--surface-0); border-left:1px solid var(--surface-border); border-top:1px solid var(--surface-border);
    transform:rotate(45deg);
  }
  .dropdown-menu li{width:100%;}
  .dropdown-menu a{
    display:block; padding:10px 12px; font-size:13.5px; font-weight:700;
    text-transform:none; letter-spacing:0.2px; color:var(--text-strong);
    border-radius:var(--radius-sm); white-space:nowrap;
  }
  .dropdown-menu a:hover{background:var(--surface-2); color:var(--red);}
  .call-btn{
    background:var(--red); color:#fff; font-size:14px; font-weight:700;
    letter-spacing:0.4px; padding:11px 22px; border-radius:var(--radius-sm);
    display:flex; align-items:center; gap:8px; text-transform:uppercase;
    white-space:nowrap;
  }
  .call-btn:hover{background:var(--red-dark);}
  .hamburger{
    display:none; flex-direction:column; justify-content:center; gap:5px;
    background:none; width:32px; height:32px; flex-shrink:0;
  }
  .hamburger span{
    width:24px; height:2px; background:var(--text-strong); display:block;
    transition:transform .2s ease, opacity .2s ease;
  }
  .hamburger.open span:nth-child(1){transform:translateY(7px) rotate(45deg);}
  .hamburger.open span:nth-child(2){opacity:0;}
  .hamburger.open span:nth-child(3){transform:translateY(-7px) rotate(-45deg);}

  /* ===== MOBILE NAV DRAWER ===== */
  .mobile-nav{
    display:none; background:var(--surface-0); border-top:1px solid var(--surface-border);
  }
  .mobile-nav.open{display:block;}
  .mobile-nav > ul{padding:6px 20px 18px;}
  .mobile-nav > ul > li{border-bottom:1px solid #EEF1F5;}
  .mobile-nav > ul > li:last-child{border-bottom:none;}
  .mobile-nav > ul > li > a{
    display:flex; align-items:center; padding:14px 2px;
    font-size:15px; font-weight:700; letter-spacing:0.3px; text-transform:uppercase;
    color:var(--text-strong);
  }
  .mobile-nav li.active > a{color:var(--red);}
  .mobile-sub-toggle{
    display:flex; align-items:center; justify-content:space-between; width:100%;
    background:none; text-align:left; padding:14px 2px;
    font-size:15px; font-weight:700; letter-spacing:0.3px; text-transform:uppercase;
    color:var(--text-strong); font-family:inherit;
  }
  .mobile-has-sub .caret{transition:transform .2s ease;}
  .mobile-has-sub.open .caret{transform:rotate(180deg);}
  .mobile-submenu{
    display:none; padding-left:10px; padding-bottom:6px;
  }
  .mobile-has-sub.open .mobile-submenu{display:block;}
  .mobile-submenu a{
    display:block; padding:11px 2px; font-size:14px; font-weight:600;
    text-transform:none; color:#555;
  }
  .mobile-submenu a:hover{color:var(--red);}

  /* ===== SHARED BUTTONS / EYEBROW LABEL ===== */
  .eyebrow{
    color:var(--red); font-size:13.5px; font-weight:800; letter-spacing:3px;
    margin-bottom:16px;
  }
  .btn{
    display:flex; align-items:center; gap:9px; font-size:15px; font-weight:800;
    letter-spacing:0.4px; text-transform:uppercase; padding:14px 26px; border-radius:var(--radius-sm);
  }
  .btn-red{background:var(--red); color:#fff;}
  .btn-red:hover{background:var(--red-dark);}
  .btn-green{background:var(--green); color:#fff;}
  .btn-green:hover{background:#117F3A;}

  /* ===== RESPONSIVE (shared header) ===== */
  /* Collapses to the mobile drawer below this width instead of wrapping the full nav. */
  @media (max-width:1024px){
    nav.main-nav > ul{display:none;}
    .hamburger{display:flex;}
    .call-btn{display:none;}
  }
  @media (max-width:760px){
    .header-inner{padding:12px 20px;}
    .logo-img{height:38px;}
    .call-btn span.txt{display:none;}
    .call-btn{padding:11px 14px;}
    .btn{padding:12px 18px; font-size:13px;}
  }
  @media (max-width:420px){
    .header-inner{padding:10px 16px;}
    .logo-img{height:32px;}
    .eyebrow{font-size:13.5px; letter-spacing:2px;}
    .mobile-nav ul{padding:6px 16px 16px;}
  }

  /* Reduced motion / focus */
  a:focus-visible, button:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible{
    outline:2px solid var(--red); outline-offset:2px;
  }

  /* ===== FOOTER ===== */
  footer{
    background:#172539;
    border-top:1px solid var(--line);
    padding:64px 32px 0;
  }
  .footer-inner{
    max-width:1280px; margin:0 auto;
    display:grid; grid-template-columns:1.3fr 1fr 1fr 1.2fr 1fr; gap:40px;
    padding-bottom:44px;
  }
  .footer-col .flogo{display:flex; align-items:center; margin-bottom:18px;}
  .footer-logo-img{height:38px; width:auto; display:block;}
  .footer-col p.about{color:var(--muted); font-size:14.5px; line-height:1.75; margin-bottom:20px; max-width:250px;}
  .footer-social{display:flex; gap:10px;}
  .footer-social a{
    width:32px; height:32px; border-radius:var(--radius-full); background:var(--charcoal-2);
    display:flex; align-items:center; justify-content:center;
    border:1px solid var(--line);
  }
  .footer-social a:hover{background:var(--red); border-color:var(--red);}

  .fhead{
    font-size:14.5px; font-weight:800; letter-spacing:0.6px; text-transform:uppercase;
    color:#fff; margin-bottom:8px; position:relative; padding-bottom:12px;
  }
  .fhead::after{
    content:""; position:absolute; left:0; bottom:0; width:100%; height:3px;
    background:var(--red); border-radius:var(--radius-sm);
  }
  .flist{margin-top:20px;}
  .flist li{margin-bottom:12px;}
  .flist a{
    font-size:14.5px; color:var(--muted); display:flex; align-items:center; gap:8px;
    transition:color .15s;
  }
  .flist a:hover{color:#fff;}
  .flist a .arrow{color:var(--red); font-size:13px; font-weight:800;}

  .fcontact{margin-top:20px;}
  .fcontact li{
    display:flex; align-items:flex-start; gap:11px; font-size:14.5px; color:var(--muted);
    margin-bottom:16px; line-height:1.55;
  }
  .fcontact li svg{flex-shrink:0; margin-top:2px;}
  .fcontact li a{color:var(--muted);}
  .fcontact li a:hover{color:#fff;}

  .fcallnow{
    display:flex; align-items:center; justify-content:center; gap:9px;
    background:var(--red); color:#fff; font-size:14px; font-weight:800;
    letter-spacing:0.4px; text-transform:uppercase; padding:14px; border-radius:var(--radius-sm);
    margin-top:22px;
  }
  .fcallnow:hover{background:var(--red-dark);}
  .fwhatsapp{
    display:flex; align-items:center; justify-content:center; gap:9px;
    background:var(--green); color:#fff; font-size:14px; font-weight:800;
    letter-spacing:0.4px; text-transform:uppercase; padding:14px; border-radius:var(--radius-sm);
    margin-top:14px;
  }
  .fwhatsapp:hover{background:#117F3A;}

  .footer-bottom{
    border-top:1px solid var(--line);
    padding:22px 0;
    text-align:center;
  }
  .footer-bottom p{font-size:13.5px; color:var(--muted); letter-spacing:0.3px;}

  @media (max-width:980px){
    .footer-inner{grid-template-columns:1fr 1fr; gap:36px;}
  }
  @media (max-width:600px){
    .footer-inner{grid-template-columns:1fr; gap:32px;}
    footer{padding:48px 20px 0;}
  }
  @media (max-width:420px){
    footer{padding:40px 16px 0;}
    .footer-logo-img{height:32px;}
    .fhead{font-size:13.5px;}
  }

  /* ===== PAGE BANNER ===== */
  .page-banner{
    position:relative;
    background:linear-gradient(150deg, var(--charcoal-2) 0%, var(--charcoal) 100%);
    overflow:hidden;
    padding:var(--space-8) var(--space-6) 58px;
    text-align:center;
  }
  .page-banner::before{
    content:""; position:absolute; inset:0;
    background-image:
      linear-gradient(rgba(255,255,255,0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.035) 1px, transparent 1px);
    background-size:44px 44px;
    mask-image:radial-gradient(ellipse 70% 70% at 50% 40%, black, transparent);
    pointer-events:none;
  }
  .page-banner-inner{position:relative; max-width:1280px; margin:0 auto;}
  .page-banner .eyebrow{justify-content:center; display:flex;}
  .page-banner h1{
    font-size:var(--text-5xl); color:#fff; letter-spacing:0.4px; margin-bottom:16px;
  }
  .page-banner h1 .accent{color:var(--red);}
  .page-banner-inner > p{color:var(--off); font-size:var(--text-base); line-height:1.75; max-width:620px; margin:0 auto 20px;}
  .breadcrumb{
    display:flex; justify-content:center; align-items:center; gap:10px;
    font-size:13px; color:var(--muted); text-transform:uppercase; letter-spacing:0.5px;
    font-weight:700;
  }
  .breadcrumb a{color:var(--off);}
  .breadcrumb a:hover{color:var(--red);}
  .breadcrumb .sep{color:var(--red); font-size:13.5px;}
  .breadcrumb .current{color:var(--red);}

  @media (max-width:760px){
    .page-banner{padding:var(--space-7) var(--space-5) 40px;}
    .page-banner h1{font-size:var(--text-3xl);}
  }
  @media (max-width:420px){
    .page-banner{padding:36px 16px 32px;}
    .breadcrumb{font-size:14px;}
  }

  /* ===== CONTACT PAGE ===== */
  .contact-info-section{background:var(--surface-1); padding:60px 32px 0;}
  .contact-info-grid{
    max-width:1280px; margin:0 auto 60px;
    display:grid; grid-template-columns:repeat(4, 1fr); gap:24px;
  }
  .contact-info-card{
    background:#fff; border-radius:var(--radius-lg); padding:32px 22px; text-align:center;
    box-shadow:var(--shadow-3);
  }
  .contact-info-icon{
    width:58px; height:58px; border-radius:var(--radius-full); margin:0 auto 18px;
    background:linear-gradient(150deg, var(--red) 0%, var(--red-dark) 100%);
    display:flex; align-items:center; justify-content:center;
  }
  .contact-info-card h3{
    font-size:var(--text-base); color:var(--text-strong); text-transform:uppercase; letter-spacing:0.4px; margin-bottom:10px;
  }
  .contact-info-card p{color:var(--text-muted); font-size:13.5px; line-height:1.7;}
  .contact-info-card a{color:var(--text-muted);}
  .contact-info-card a:hover{color:var(--red);}

  .contact-main-section{background:var(--surface-1); padding:0 32px 64px;}
  .contact-main-layout{
    max-width:1280px; margin:0 auto;
    display:grid; grid-template-columns:1.15fr 0.85fr; gap:48px; align-items:start;
  }

  .contact-form-card{
    background:#fff; border-radius:var(--radius-lg); padding:38px 34px;
    box-shadow:var(--shadow-3);
  }
  .contact-form-card h2{font-size:var(--text-3xl); color:var(--text-strong); margin-bottom:8px; letter-spacing:0.2px;}
  .contact-form-card > p{color:var(--text-muted); font-size:13.5px; margin-bottom:26px;}

  .form-row{display:grid; grid-template-columns:1fr 1fr; gap:18px;}
  .form-group{display:flex; flex-direction:column; gap:8px; margin-bottom:18px;}
  .form-group label{
    font-size:13px; font-weight:700; color:var(--text-strong); text-transform:uppercase; letter-spacing:0.3px;
  }
  .form-group input, .form-group select, .form-group textarea{
    border:1.5px solid var(--surface-border); border-radius:var(--radius-md); padding:12px 14px;
    font-family:inherit; font-size:13.5px; color:var(--text-strong); background:#fff;
  }
  .form-group input:focus, .form-group select:focus, .form-group textarea:focus{
    outline:none; border-color:var(--red);
  }
  .form-group textarea{resize:vertical; min-height:120px;}

  .form-submit-btn{
    background:var(--red); color:#fff; font-size:13.5px; font-weight:800;
    letter-spacing:0.5px; text-transform:uppercase; padding:14px 30px; border-radius:var(--radius-md);
    display:inline-flex; align-items:center; gap:10px;
  }
  .form-submit-btn:hover{background:var(--red-dark);}
  .form-submit-btn:disabled{opacity:0.6; cursor:not-allowed;}

  .form-success{
    display:none; align-items:center; gap:10px;
    background:#EAF6EE; border:1.5px solid var(--green); border-radius:var(--radius-md);
    padding:14px 18px; color:#1c7a41; font-size:13px; margin-top:18px;
  }
  .form-success.show{display:flex;}

  .contact-side{display:flex; flex-direction:column; gap:26px;}

  .map-card{border-radius:var(--radius-lg); overflow:hidden; box-shadow:var(--shadow-3); line-height:0; position:relative;}
  .map-card iframe{width:100%; height:300px; border:none; display:block;}
  .map-directions-link{
    position:absolute; inset:0; z-index:2; display:flex; align-items:flex-end;
    text-decoration:none;
  }
  .map-directions-bar{
    width:100%; background:var(--red); color:#fff; font-size:13px; font-weight:800;
    letter-spacing:0.5px; text-transform:uppercase; padding:14px;
    display:flex; align-items:center; justify-content:center; gap:8px;
    transition:background-color .2s ease;
  }
  .map-directions-link:hover .map-directions-bar{background:var(--red-dark);}

  .hours-card{
    background:#fff; border-radius:var(--radius-lg); padding:28px 26px;
    box-shadow:var(--shadow-3);
  }
  .hours-card h3{
    font-size:14.5px; color:var(--text-strong); text-transform:uppercase; letter-spacing:0.4px;
    margin-bottom:18px; padding-bottom:14px; position:relative;
  }
  .hours-card h3::after{
    content:""; position:absolute; left:0; bottom:0; width:36px; height:3px;
    background:var(--red); border-radius:var(--radius-sm);
  }
  .hours-row{
    display:flex; justify-content:space-between; padding:10px 0;
    border-bottom:1px solid var(--surface-border); font-size:13px;
  }
  .hours-row:last-child{border-bottom:none; padding-bottom:0;}
  .hours-row .day{color:var(--text-strong); font-weight:700;}
  .hours-row .time{color:var(--text-muted);}
  .hours-row.closed .time{color:var(--red); font-weight:700;}

  .social-card{
    background:linear-gradient(150deg, var(--charcoal-2) 0%, var(--charcoal) 100%);
    border-radius:var(--radius-lg); padding:28px 26px; text-align:center; position:relative; overflow:hidden;
  }
  .social-card::before{
    content:""; position:absolute; inset:0;
    background-image:
      linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
    background-size:36px 36px;
    mask-image:radial-gradient(ellipse 70% 80% at 50% 40%, black, transparent);
  }
  .social-card h3{
    position:relative; color:#fff; font-size:14.5px; text-transform:uppercase;
    letter-spacing:0.4px; margin-bottom:18px;
  }
  .social-card .footer-social{position:relative; justify-content:center;}

  @media (max-width:1024px){
    .contact-info-grid{grid-template-columns:repeat(2, 1fr);}
  }
  @media (max-width:980px){
    .contact-main-layout{grid-template-columns:1fr;}
  }
  @media (max-width:760px){
    .contact-info-section{padding:44px 20px 0;}
    .contact-main-section{padding:0 20px 44px;}
    .contact-form-card{padding:28px 22px;}
    .form-row{grid-template-columns:1fr;}
  }
  @media (max-width:460px){
    .contact-info-grid{grid-template-columns:1fr;}
  }
</style>




<section class="page-banner">
  <div class="page-banner-inner">
    <div class="eyebrow reveal">GET IN TOUCH</div>
    <h1 class="display reveal">CONTACT <span class="accent">US</span></h1>
    <p class="reveal">Questions about LPG &amp; CNG conversion, hybrid service, or booking a repair? Call, WhatsApp, or send us a message — we typically reply within 24 hours.</p>
    <div class="breadcrumb reveal">
      <a href="index.html">Home</a>
      <span class="sep">»</span>
      <span class="current">Contact Us</span>
    </div>
  </div>
</section>

<section class="contact-info-section">
  <div class="contact-info-grid">

    <div class="contact-info-card reveal">
      <div class="contact-info-icon">
        <svg class="icon" width="24" height="24" style="color:#fff"><use href="#icon-location"/></svg>
      </div>
      <h3>Our Location</h3>
      <p>Sector # 15, Block # C, Road # 3/A,<br>Plot # 40, Diabari, Uttara, Dhaka.</p>
    </div>

    <div class="contact-info-card reveal">
      <div class="contact-info-icon">
        <svg class="icon" width="22" height="22" style="color:#fff"><use href="#icon-phone"/></svg>
      </div>
      <h3>Call Us</h3>
      <p><a href="tel:+8801854226757">01854226757</a><br>Sat &ndash; Thu, 9AM &ndash; 8PM</p>
    </div>

    <div class="contact-info-card reveal">
      <div class="contact-info-icon">
        <svg class="icon" width="24" height="24" style="color:#fff"><use href="#icon-email"/></svg>
      </div>
      <h3>Email Us</h3>
      <p><a href="mailto:mosharrafhossen090@gmail.com">mosharrafhossen090@gmail.com</a><br>We reply within 24 hours</p>
    </div>

    <div class="contact-info-card reveal">
      <div class="contact-info-icon">
        <svg class="icon" width="24" height="24" style="color:#fff"><use href="#icon-clock"/></svg>
      </div>
      <h3>Working Hours</h3>
      <p>Saturday &ndash; Thursday<br>9:00 AM &ndash; 8:00 PM</p>
    </div>

  </div>
</section>

<section class="contact-main-section">
  <div class="contact-main-layout">

    <div class="contact-form-card reveal">
      <h2>Send Us a <span style="color:var(--red);">Message</span></h2>
      <p>Have a question about a service or want to book an appointment? Fill out the form below and our team will get back to you.</p>

      <form id="contactForm">
        <div class="form-row">
          <div class="form-group">
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" name="fullName" placeholder="Your name" required>
          </div>
          <div class="form-group">
            <label for="phoneNumber">Phone Number</label>
            <input type="tel" id="phoneNumber" name="phoneNumber" placeholder="01XXX-XXXXXX" required>
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="emailAddress">Email Address</label>
            <input type="email" id="emailAddress" name="emailAddress" placeholder="you@example.com" required>
          </div>
          <div class="form-group">
            <label for="serviceInterest">Service Interested In</label>
            <select id="serviceInterest" name="serviceInterest">
              <option value="">Select a service</option>
              <option value="lpg-cng">LPG &amp; CNG Conversion</option>
              <option value="engine">Engine Repair &amp; Service</option>
              <option value="ac">Car AC Service</option>
              <option value="hybrid">Hybrid Vehicle Service</option>
              <option value="carwash">Car Wash &amp; Detailing</option>
              <option value="other">Other</option>
            </select>
          </div>
        </div>

        <div class="form-group">
          <label for="message">Message</label>
          <textarea id="message" name="message" placeholder="Tell us about your vehicle and what you need..." required></textarea>
        </div>

        <button type="submit" class="form-submit-btn" id="formSubmitBtn">
          <span id="formSubmitBtnText">Send Message</span>
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none"><path d="M22 2L11 13" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M22 2l-7 20-4-9-9-4 20-7z" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>

        <div class="form-success" id="formSuccess">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="#22A24D" stroke-width="1.8"/><path d="M8 12.5l2.5 2.5L16 9" stroke="#22A24D" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          <span>Thank you! Your message has been received. We'll get back to you shortly.</span>
        </div>
        <div class="form-success" id="formError" style="background:#FDECEC; border-color:#F5B4B4;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="#D14343" stroke-width="1.8"/><path d="M12 7v6M12 16.5v.01" stroke="#D14343" stroke-width="2" stroke-linecap="round"/></svg>
          <span>Sorry, something went wrong sending your message. Please call us directly at <a href="tel:+8801854226757" style="color:#D14343; font-weight:700;">01854226757</a> or try again.</span>
        </div>
      </form>
    </div>

    <div class="contact-side">

      <div class="map-card reveal">
        <a href="https://www.google.com/maps/dir/?api=1&destination=23.8657374,90.3658627" target="_blank" rel="noopener" class="map-directions-link" aria-label="Get directions to Taqwa Automobile Service Center">
          <div class="map-directions-bar">
            <svg class="icon" width="15" height="15"><use href="#icon-directions"/></svg>
            Get Directions
          </div>
        </a>
        <iframe src="https://www.google.com/maps?q=23.8657374,90.3658627&z=17&output=embed" loading="lazy" referrerpolicy="no-referrer-when-downgrade" title="Taqwa Automobile Service Center Location"></iframe>
      </div>

      <div class="hours-card reveal">
        <h3>Business Hours</h3>
        <div class="hours-row"><span class="day">Saturday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row"><span class="day">Sunday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row"><span class="day">Monday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row"><span class="day">Tuesday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row"><span class="day">Wednesday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row"><span class="day">Thursday</span><span class="time">9:00 AM &ndash; 8:00 PM</span></div>
        <div class="hours-row closed"><span class="day">Friday</span><span class="time">Closed</span></div>
      </div>

      <div class="social-card reveal">
        <h3>Follow Us</h3>
        <div class="footer-social">
          <a href="#" aria-label="Facebook"><svg class="icon" width="15" height="15"><use href="#icon-facebook"/></svg></a>
          <a href="#" aria-label="YouTube"><svg class="icon" width="16" height="16"><use href="#icon-youtube"/></svg></a>
          <a href="#" aria-label="Instagram"><svg class="icon" width="15" height="15"><use href="#icon-instagram"/></svg></a>
        </div>
      </div>

    </div>

  </div>
</section>


<script>
  // ===== CONTACT FORM (submits via FormSubmit.co - no server needed) =====
  // NOTE: the first real submission sends a one-time confirmation email to
  // mosharrafhossen090@gmail.com — that link must be clicked once to activate delivery.
  var contactForm = document.getElementById('contactForm');
  var formSuccess = document.getElementById('formSuccess');
  var formError = document.getElementById('formError');
  var submitBtn = document.getElementById('formSubmitBtn');
  var submitBtnText = document.getElementById('formSubmitBtnText');
  var FORM_ENDPOINT = 'https://formsubmit.co/ajax/mosharrafhossen090@gmail.com';

  if (contactForm) {
    contactForm.addEventListener('submit', function(e){
      e.preventDefault();
      if(!contactForm.checkValidity()){
        contactForm.reportValidity();
        return;
      }

      formSuccess.classList.remove('show');
      formError.classList.remove('show');
      submitBtn.disabled = true;
      submitBtnText.textContent = 'Sending...';

      var formData = new FormData(contactForm);
      var payload = {
        _subject: 'New enquiry from taqwaautomobile.com contact form',
        Name: formData.get('fullName'),
        Phone: formData.get('phoneNumber'),
        Email: formData.get('emailAddress'),
        'Service Interested In': formData.get('serviceInterest') || 'Not specified',
        Message: formData.get('message')
      };

      fetch(FORM_ENDPOINT, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
        body: JSON.stringify(payload)
      })
        .then(function(res){ if(!res.ok){ throw new Error('Form submission failed'); } return res.json(); })
        .then(function(){
          formSuccess.classList.add('show');
          contactForm.reset();
          formSuccess.scrollIntoView({behavior:'smooth', block:'center'});
        })
        .catch(function(){
          formError.classList.add('show');
          formError.scrollIntoView({behavior:'smooth', block:'center'});
        })
        .finally(function(){
          submitBtn.disabled = false;
          submitBtnText.textContent = 'Send Message';
        });
    });
  }
</script>
</Layout>
